[project]
name = "playwright-shoppingkart"
version = "0.1.0"
description = ""
authors = [
    {name = "Praveen S",email = "51072726+i-ampraveen@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright (>=1.52.0,<2.0.0)",
    "selenium (>=4.32.0,<5.0.0)",
    "pytest-bdd (>=8.1.0,<9.0.0)",
    "openpyxl (>=3.1.5,<4.0.0)",
    "pytest-playwright (>=0.7.0,<0.8.0)",
    "faker (>=37.3.0,<38.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
allure-pytest = "^2.14.2"
pytest-html = "^4.1.1"
pytest-xdist = "^3.6.1"

